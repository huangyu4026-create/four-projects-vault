name: Deploy public mobile pages

on:
  push:
    branches:
      - main
    paths:
      - "work-life-mobile/**"
      - "digital-life-public/**"
      - ".github/workflows/work-life-mobile-pages.yml"
      - ".nojekyll"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            work-life-mobile
            digital-life-public
          sparse-checkout-cone-mode: true

      - name: Prepare static site
        run: |
          mkdir -p _site/work-life-mobile _site/digital-life-public
          cp -R work-life-mobile/. _site/work-life-mobile/
          cp -R digital-life-public/. _site/digital-life-public/
          touch _site/.nojekyll

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
