# 红楼梦正式取材专库双轨铁门规则

## 一句话

双轨铁门：坐标只走坐标中心库，语义只走语义聚拢中心库。

## 两个入口

- 坐标查询入口：`/Users/yu/Documents/Codex/2026-06-21/new-chat-3/outputs/红楼梦正式取材专库/00_双中心取材总库/02_坐标查询中心库/红楼梦坐标查询中心库_CH001_120.sqlite`
- 语义 / 聚拢查询入口：`/Users/yu/Documents/Codex/2026-06-21/new-chat-3/outputs/红楼梦正式取材专库/00_双中心取材总库/01_语义聚拢查询中心库/红楼梦语义聚拢中心库_CH001_120.sqlite`

## 机器硬规则

1. `recall_gate=coordinate` 或 `source_scope=formal_coordinate_db` 时，`source_db` 必须等于坐标查询中心库。
2. `recall_gate=semantic` 或 `source_scope=formal_axis_db` 时，`source_db` 必须等于语义聚拢中心库。
3. 坐标路不得读语义库，语义路不得读坐标库。
4. 登记处、词位库、字位桥表、人物归一库、旧全文库、场面时间库只作辅助、回源、归一、补点或流程规则，不作前台候选来源。
5. 工程运行包、闭环包、报告、过程 JSON、日志不得作为候选来源。

## 最终原则

中心库命中仍不是最终答案；最终答案必须回原文裁判。
