# 数字生命全量对标工程（2026-06-20）

## 0. 这次做了什么

按红楼梦工程的做法，把数字生命从“旧 Notion/旧本地 OS/Coex 迁移包/恢复包/输出区”重新接成一张可核验的工程坐标。

本次产物只做派生索引和修复清单：

- 不修改 Notion 母本。
- 不修改 SQLite 主库或稳定快照。
- 不删除旧文件。
- 不把 GitHub 试运行包或恢复包当作当前权威，只做来源证据。

## 1. 结论先行

- 数字生命材料已经迁入 Coex，DL 域当前扫描到 `2860` 个文件。
- 旧入口和旧总账里的绝对路径仍大量指向 `/Users/yu/Documents/Codex/2026-06-03/...` 与 `/Users/yu/Documents/Codex/2026-06-05/...`。
- 本次从关键入口/总账/运行清单中抽取 `151` 条绝对路径引用，其中 `124` 条当前旧路径失联。
- 失联不等于材料丢失：多数材料在 Coex 的 `来源路径保留` 下存在，需要把入口和总账改成“旧来源路径 + Coex现址”的双列坐标。

## 2. 本包文件

- `01_红楼梦方法抽取.md`：本次复用的方法论。
- `02_DL01-DL40模块覆盖矩阵_20260620.csv/json/md`：数字生命版模块坐标。
- `03_文件级全量对标索引_20260620.csv/json`：DL 域所有文件级映射。
- `04_旧入口引用路径修复矩阵_20260620.csv/json/md`：旧路径失联与 Coex 推荐现址。
- `05_动作规则对标矩阵_20260620.csv/md`：触发词、规则、入口、队列、安全链路等动作规则索引。
- `06_P0_P1落地执行清单_20260620.csv/json/md`：下一步修复队列。
- `07_生成审计.json`：生成统计和边界。

## 3. 使用口径

以后数字生命新增任何页面、文件、动作、规则，先进入 `DL01-DL40` 模块坐标；找不到坐标的进入 `DL39 异常镜像与缺口队列`，不要直接塞进入口或工作流。
