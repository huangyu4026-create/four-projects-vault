# 002C_旧语义聚拢头逐页搬迁打勾清单

生成时间：2026-06-21

用途：逐页核对旧语义 / 聚拢头部从进门第一步到材料池前的所有关键文件和内容块，标明哪些已经搬到坐标头，哪些没有搬，为什么没有搬。

结论先写清楚：

```text
旧头不是整体复制到坐标头。
坐标头只复制“入口纪律、AI取词心法、人物归一、证据状态、材料池后身”。
坐标头不复制“旧闭环包四件套作为第一取材动作、聚拢总图节点表作为第一裁判、旧语义召回作为第一入口”。
```

## 一、总完成度

| 类别 | 数量 | 状态 |
|---|---:|---|
| 已完整搬入 | 9 | 纪律、状态、取词心法、人物归一、物象根字、材料池后身等已进入 `000C` 和坐标材料门 |
| 已改名 / 改工具搬入 | 7 | 旧“聚拢图内动作”已改成坐标的词位、变量、字位、距离、共场、交集 |
| 故意不搬 | 5 | 旧闭环包前置、00AC/00AG/00AI/00AM 前置、113/118 图表实体、旧语义召回、旧搜索词网络 |
| 已补强但待试跑验收 | 2 | 前台短卡、坐标候选入材料池凭证门已补；还要用真实线程试跑核验 |

一句话：

```text
方法和纪律基本搬完。
旧取材实体没有搬，因为那会把坐标头重新吸回聚拢头。
剩下风险不在理论，而在“坐标材料包 -> 坐标入材料池凭证门 -> 材料池后身”的试跑验收。
```

## 二、逐页打勾矩阵

| 编号 | 旧语义 / 聚拢头文件或内容块 | 旧头作用 | 坐标头对应位置 | 状态 | 举证 | 没搬 / 改搬原因 |
|---|---|---|---|---|---|---|
| 0 | `000_进入红楼梦工程_新窗口强制入口.md` | 旧总入口；禁止自由搜索；先建闭环包；四件套验进入聚拢库 | `000C_进入坐标查询_新窗口强制入口.md` | ✅纪律已搬，❌闭环包前置不搬 | `000C` 有“总铁律”“继承铁律”“禁止事项”“坐标头验收矩阵” | `formal_honglou_closed_loop.py` 不能作为坐标第一动作，否则坐标头会被旧闭环包吸走 |
| 1 | `131_红楼梦工程唯一运行顺序与双打卡验收基线.md` | 固定 0-15 唯一顺序；先闭环包、四件套、126、130、119、118、原文、材料池 | `000C` 的“坐标头强制流程”和“与旧头对应关系” | ✅顺序思想已搬，🔁前半段替换 | `000C` 明确 0-11 坐标流程；`001C` 第六节有完整运行矩阵 | 旧 0A/0B 闭环包与四件套改成坐标分流令牌和坐标预检 |
| 2 | `127_进入红楼梦工程打卡门_必读已加载清单.md` | 新窗口短卡；外显 package_dir、四件套、入口词 | `000C` 的“坐标头输出格式”“状态词” | 🔁已改搬 | `000C` 要求输出取材门、recall_gate、Codex查询词、人物归一状态、材料命中字段 | 坐标头不显示旧 package_dir / 00AC 四件套作为前半段凭证，改看坐标字段 |
| 3 | `128_聚拢库总入口_全库地图与流程记录.md` | 全库地图；相关库深盘后全部回聚拢库 | 坐标头的三库地图：词位库、变量投影库、字位桥表 | 🔁已改搬 | `000C` 写明全文词位库 / 全量变量投影库 / 全文字位桥表；`001C` 第五节列坐标新增项 | 不复制旧全库地图全文；坐标头只需要新的取材主库，后身再回旧材料池 |
| 4 | `117_聚拢总图新入口SOP与触发词同步包.md` | 抽入口线索，进聚拢总图，放大缩小交集串域，回原文 | `000C` 的 AI 取词硬规则与坐标取材流程 | ✅方法已搬，🔁工具替换 | `000C` 写 AI 取词 -> Codex查询词 / 强复合 / 词角色 -> 坐标材料门 | “进聚拢总图”改成“进坐标材料门”；动作保留为距离、共场、交集、容器 |
| 5 | `119_聚拢总图图内读法与问题思维方式.md` | 教 AI 进图后判断节点身份、标签身份、盘法、原文裁判 | 坐标头的 atom / variable / container / distance 读法 | 🔁已改搬 | `001C` 第四-C 把关系强度改为 same_atom / same_scene / same_event / same_chapter / distance / cooccurrence | 坐标法没有旧 node_id / closure / line_membership 的图读法，改读原子段和容器层级 |
| 6 | `120_聚拢总图中段替换与旧工具吸纳硬规则.md` | 旧工具不丢，但全部回聚拢总图；穷尽法作为补点工具 | 坐标头保留穷尽法、人物归一、材料池后身，但换第一工具 | ✅思想已搬，🔁旧工具分层 | `000C` 有“词位轴”“对象词提醒”；`formal_honglou_coordinate_material_gate.py` 输出 `coordinate_exhaustive_status` | 旧“聚拢总图唯一中段”在坐标头前半段不再是唯一中段，坐标材料门是新前半段 |
| 7 | `126_聚龙法查询词入口规范_先归一再宽网.md` | 人物先归一；人物不拆；物象、小词、动作查根字；疑义宽网 | `000C` 的“AI取词硬规则”“继承聚拢查询取词心法”；`001C` 四-B | ✅已重点搬入 | `000C` 写人名不拆不猜、70/72人物归一、竹/金/玉/泪/帕/灯/花/黄等单字物象可作主查词 | 这是前面漏得最严重的部分，现已补签进矩阵和坐标头 |
| 8 | `130_外部判题与入图盘面双打卡门_硬入口软退回.md` | 0号闭环包凭证；外部判题；入口词与入图盘面；红黄绿灯 | 坐标预检令牌：坐标入口、入口词、人物归一、强复合、查证顺序、材料升级条件 | 🔁已改搬 | `formal_honglou_coordinate_material_gate.py` 有 `coordinate_preflight_tokens()`；`000C` 有“坐标头验收矩阵” | 旧 0 号闭环包不搬；红黄绿灯改成通过 / 需补证 / 阻断 |
| 9 | `130_后台重前台轻_四项优化落地记录.md` | 机器短卡、证据分层、反证排除，不加重前台 | 坐标材料包的 `00_入口令牌.json`、`01_查询词路.md`、`02_坐标材料池入口候选.json`、`03_材料池入口_候选不作答.md`、`04_坐标入材料池凭证门.md/json` | ✅已补强，待试跑 | `formal_honglou_coordinate_material_pack_cli.py` 固定生成坐标轻量包和入池凭证门；`000C` 已写坐标短卡 | 前台是否稳定显示短卡还要试跑，但工程字段已补 |
| 10 | `116_旧前门硬封锁执行令_机器拦截规则.md` | 旧失败前门退场，禁止 AI 被旧词吸回旧路 | 坐标触发词改成 `进入坐标查询`，避免 `进入红楼梦工程` | ✅已搬 | `000C` 明确不要写“进入红楼梦工程 / 进入红楼梦坐标工程” | 这是触发词层面的硬搬迁，目的是绕开旧 000 的吸力 |
| 11 | `113_编号递归聚拢总图_*` | 旧聚拢总图实体：nodes、edges、tags、closure、line_membership | 坐标库实体：atom_coordinates、variable_points、container_variable_index、distance views | ❌不搬实体，🔁换底座 | 坐标库已经另建为全量变量投影库、词位库、字位桥表 | 113 是旧聚拢实体底座；坐标头第一段不用它，避免两个底座打架 |
| 12 | `118_万库映射回聚拢总图_*` | 旧万库映射回聚拢总图，强证/候选/需复审三档 | 坐标材料门的 evidence_grades、review_status、direct/scene/event/chapter 层级 | 🔁部分搬入 | `formal_honglou_coordinate_material_gate.py` 输出 `coordinate_evidence_grades`、`material_warnings`、`material_status` | 旧三档表实体不搬；坐标用变量点证据等级和材料警告代替 |
| 13 | `formal_honglou_codex_recall.py` 中查询词路心法 | 定中心、定路径、定词网、定执行、定资料化问题；term_roles | `000C` 与 `001C` 四-A、四-E | ✅已补搬 | `001C` 四-A 和四-E 已逐项映射 `question_center/search_terms/compound_groups/term_roles/source_order/guardrails` | 不搬成机器编译器；用户已明确不要编译器，要 AI 取词 |
| 14 | 旧 `formal_honglou_closed_loop.py --question` | 旧聚拢路线第一机器动作，产出 00AC/00AG/00AI/00AM | 坐标路线改用 `formal_honglou_coordinate_material_pack_cli.py --question ... --route-context ...` | 🔁替换完成 | `000C` 固定工具；CLI 缺 `Codex查询词` 会拒绝 | 旧脚本不能接在坐标第一步，否则分流失败 |
| 15 | 旧材料池四态 / 00I / 00L / 00M / 红楼解语 | 材料入池后裁判、精读、写作前追证、最终答案 | 坐标头后半身完全共用 | ✅已搬 | `000C` 多处写“同一个材料池入口”“材料池四态”“精读材料词”“写作前追证” | 这部分不需要重写，只要坐标候选能进池 |
| 16 | 旧“命中不是答案”状态机 | 召回已命中 -> 待聚拢裁判；不得直接写答案 | 坐标状态机：坐标取材已命中 -> 待入池 / 待材料池四态 | ✅已搬 | `000C` 的“状态词”和“写作阻断”；`001C` 总判断矩阵 | 坐标命中同样只是候选 |
| 17 | 旧“查询词不是答案” | 入口词只撒网，不能放行结论 | 坐标 `Codex查询词` 只服务取材 | ✅已搬 | `000C` 的“继承铁律”“AI取词硬规则”；`001C` 第四节 | 无 |

## 三、已补上的原缺口与剩余验收点

### 1. 坐标候选接回材料池后身：已补入池凭证门，待试跑验收

现在不再只说“送入材料池入口”，而是补成工程可读的坐标凭证门：

```text
04_坐标入材料池凭证门.md
04_坐标入材料池凭证门.json
```

凭证门写清：

```text
material_admission_status：准入待判 / 阻断待补
codex_query_lane：coordinate
source_system：坐标材料门
source_trace：词位库 / 变量投影库 / 字位桥表 / 距离共场
atom_code / segment_no / chapter_no / source_sentence
```

当前状态：

```text
坐标取材包：已能生成。
坐标入材料池凭证门：已补。
材料池后身规则：已写明共用。
真实线程自动衔接：待试跑验收。
```

### 2. 前台短卡：已补坐标短卡，待试跑验收

`000C` 已补坐标短卡，要求新页面只显示轻量状态：

```text
已触发坐标查询。
取材门：坐标。
recall_gate：coordinate。
Codex查询词：____。
Codex强复合：____。
人物归一：通过 / 未触发 / 需补。
坐标预检：通过 / 需补证。
坐标取材包：package_dir=____。
坐标入材料池凭证门：准入待判 / 阻断待补。
下一步：材料池四态判定，不写最终答案。
```

剩下不是没写，而是要看新线程实际会不会稳定照这个短卡输出。

### 3. 旧 113 / 118 图实体：故意不搬

这不是漏。

坐标头的意义就是不要第一步再进旧聚拢图实体，而是先从坐标三库取材。

```text
旧图实体：材料池后身和旧聚拢路线继续用。
坐标路线：第一段不用 113/118 作为取材入口。
```

### 4. 机器自动取词编译器：已经删除，不再搬

这也不是漏。

用户已经明确：

```text
不要编译器。
让 AI 取词。
工具只验词、归一、查表、入材料池。
```

所以旧取词心法搬进提示和矩阵，不搬成自动切词机器。

## 四、最后口径

现在这件事应当这样理解：

```text
坐标头已经不是空头。
它已经搬了旧头的入口纪律、取词心法、人物归一、物象根字、补查逻辑、证据状态和材料池后身。

坐标头也不是旧头复制品。
它故意不搬旧闭环包前置、聚拢四件套前置、旧聚拢总图实体前置、旧语义召回前置。

剩余工作不是再搬理论，而是试跑衔接：
进入坐标查询 -> AI取词 -> 坐标材料门 -> 坐标材料候选 -> 04_坐标入材料池凭证门 -> 材料池四态。
```
